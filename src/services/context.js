import React from 'react';

const PlayersContext = React.createContext(null);
const HistoryContext = React.createContext(null);
const EventBonusContext = React.createContext(null);
const AuthContext = React.createContext(null);

export { PlayersContext, HistoryContext, AuthContext, EventBonusContext };
